import styled from "styled-components";

const EnrollmentContainer = styled.div`
  display: inline-block;
`;

const EventDetailContainer = styled.div`
  display: inline-block;
`;

export { EnrollmentContainer, EventDetailContainer };
